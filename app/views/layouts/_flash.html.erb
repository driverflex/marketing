<% flash.each do |key, value| %>
  <div class="flash active <%= key %>">
  	<div class="container">
  		<%= value %>
  		<%= inline_svg "exit.svg", class: "close-flash" %>
  	</div>
  </div>
<% end %>
